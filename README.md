# Next-Zustand-Query

Learning **React Query + Zustand** the right way with clean and scalable Next.js patterns.

This repository is a learning project to master modern **state management** in Next.js applications, combining **server state** (React Query) and **client state** (Zustand) efficiently.